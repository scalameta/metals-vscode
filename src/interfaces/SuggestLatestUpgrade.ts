export enum SuggestLatestUpgrade {
  /** Never suggest upgrades */
  Off = "off",
  /** Suggest upgrades with a notification popup */
  On = "on",
  /** Automatically install latest version without asking */
  Install = "install",
}
